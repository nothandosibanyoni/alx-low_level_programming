#include <stdio.h>
/**
 * main - entry point
 * return: always 0
 */

int main(void)
{
	char q;

	int a;

	q = 'a';
	a = 0;
	while
		(a < 10) {
			putchar(a + '0');
			a++;
		}
	while 
		(q <= 'f') {
			putchar(q);
			q++;
		}
	putchar('\n');
	return (0);
}
